# Core ML & Vision (CPU-only wheels — Cloud Run has no GPU)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.0+cpu
torchvision==0.18.0+cpu
numpy>=1.24.0
scikit-learn>=1.3.0
opencv-python-headless
grad-cam
pillow>=10.0.0
tqdm>=4.65.0

# API Layer (FastAPI)
fastapi
uvicorn
python-multipart
requests
python-dotenv

# AI Orchestration (LangChain/RAG/Gemini)
langchain
langchain-google-genai
langchain-pinecone

# Azure Persistence
azure-cosmos
azure-storage-blob
azure-identity
